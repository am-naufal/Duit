<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema aplikasi -->
    <style name="Theme.DuitPribadi" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Splash bawaan sistem (androidx.core:core-splashscreen).
         Tampil ±instan saat cold start, lalu di-handover ke SplashScreen Compose. -->
    <style name="Theme.DuitPribadi.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_duit_logo</item>
        <item name="windowSplashScreenAnimationDuration">400</item>
        <item name="postSplashScreenTheme">@style/Theme.DuitPribadi</item>
    </style>
</resources>
